Install npm using your OS package manager
clone repo/unzip and run "npm install" in the target directory to install dependencies
run "npm start server" and "npm run dev" in another window to host locally.

OPTIONALLY:
For natural language schedule generation in employee login, put variable "GEMINI_API_KEY=[gemini key]" in .env.
